extends layout
include mixins/movie-card

block content
  h1 Movies
  div(class='row')
    a(class='btn btn-success' href='/add-movie') Add Movie
  div(class='row')
    each movie in movies
      +movieCard(movie)
